# ================================================
# 환경변수 설정 예시
# .env.local 파일을 생성하고 아래 값들을 설정하세요
# ================================================

# 데이터베이스 (Supabase)
# Supabase: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# Direct connection: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# Supabase 설정
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# JWT 인증 (최소 32자 이상의 안전한 문자열 사용)
# 생성 명령: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-minimum-32-chars"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# 결제 (Toss Payments)
# 테스트: test_로 시작, 프로덕션: live_로 시작
TOSS_SECRET_KEY="test_sk_..."
NEXT_PUBLIC_TOSS_CLIENT_KEY="test_ck_..."

# 애플리케이션 설정
NEXT_PUBLIC_API_URL="http://localhost:3000"  # 프로덕션: https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"  # 프로덕션: https://your-domain.vercel.app
NODE_ENV="development"  # development, test, production

# Redis (Vercel KV 또는 Upstash Redis)
# Vercel KV 사용시
KV_URL="redis://default:password@host:6379"
KV_REST_API_URL="https://your-redis-endpoint.upstash.io"
KV_REST_API_TOKEN="your-redis-token"
KV_REST_API_READ_ONLY_TOKEN="your-redis-read-only-token"

# 일반 Redis 사용시
REDIS_URL="redis://username:password@host:6379/0"

# AWS S3 (선택사항 - 파일 업로드용)
AWS_REGION="ap-northeast-2"
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_S3_BUCKET=""

# 이메일 (선택사항 - 알림용)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"

# Google Translate API (선택사항 - 자동 번역용)
GOOGLE_TRANSLATE_API_KEY=""  # Google Cloud Console에서 발급

# 보안 설정 (프로덕션 권장)
NEXTAUTH_URL="http://localhost:3000"  # 프로덕션: https://your-domain.com
NEXTAUTH_SECRET=""  # openssl rand -base64 32로 생성
ENCRYPTION_KEY=""  # 32자 암호화 키 - openssl rand -hex 16
RATE_LIMIT_WINDOW_MS="900000"  # 15분
RATE_LIMIT_MAX_REQUESTS="100"  # 최대 요청 수
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001"  # CORS 허용 도메인

# ================================================
# 중요 사항:
# 1. JWT_SECRET과 JWT_REFRESH_SECRET은 반드시 다른 값 사용
# 2. 프로덕션에서는 모든 시크릿을 안전한 값으로 변경
# 3. .env.local 파일은 절대 git에 커밋하지 마세요
# ================================================